services:

  prometheus:
    image: prom/prometheus
    restart: unless-stopped

    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    restart: unless-stopped

    ports:
      - "3000:3000"

    volumes:
      - grafana:/var/lib/grafana

volumes:
  grafana: